services:
  keybroker-server:

    build: .
    
    container_name: keybroker-server

    volumes:
      - ./:/app 
    
    env_file:
      - ./.env
    
    command: uvicorn main:app --host ${UVICORN_ADDR} --port ${UVICORN_PORT} --reload

    restart: always 

    ports:
      - "${PORTS}"